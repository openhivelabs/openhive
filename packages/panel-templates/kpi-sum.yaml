id: kpi-sum
name: 합계 KPI
description: 숫자 필드의 합계를 표시 (예 이번 달 매출, 파이프라인 총액).
icon: "💰"
category: general
block:
  type: kpi
  colSpan: 1
  rowSpan: 1
  props:
    hint: ""
binding_skeleton:
  source:
    kind: team_data
    config:
      sql: "SELECT * FROM customer"
  map:
    value: value
    aggregate: sum
  refresh_seconds: 60
ai_prompts:
  goal: "숫자 필드의 합을 구해 KPI 로 보여준다."
  requires: []
