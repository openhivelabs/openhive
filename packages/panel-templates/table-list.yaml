id: table-list
name: 테이블 목록
description: 지정한 컬럼들을 표로 표시. 고객 리스트, 할 일 리스트 등 범용.
icon: "📋"
category: general
block:
  type: table
  colSpan: 3
  rowSpan: 2
  props: {}
binding_skeleton:
  source:
    kind: team_data
    config:
      sql: "SELECT * FROM customer ORDER BY id DESC LIMIT 100"
  map:
    columns: ["name", "email", "stage", "value"]
  refresh_seconds: 60
ai_prompts:
  goal: "선택한 필드들을 표로 보여준다. 고객/거래/이슈 리스트에 적합."
  requires: []
